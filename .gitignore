vendor/
node_modules/
composer.lock
.phpunit.cache/
.phpunit.result.cache
.idea/
.vscode/
*.log
.env
.env.*
!.env.example
# apps/demo/storage/ and apps/demo/bootstrap/cache/ contain their own
# .gitignore stubs that ignore all contents but keep the dir tracked,
# so we don't blanket-exclude them here.
