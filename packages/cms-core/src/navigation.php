<?php

declare(strict_types=1);

// cms-core renders pages; the admin UI for editing them lives in acme/cms-admin
// (M3) and contributes its own navigation entries there. This file is kept
// intentionally empty so cms-core can be installed without cms-admin without
// dangling route references.
return [];
